pre-commit:
  commands:
    biome:
      glob: "*.{ts,js,json,astro}"
      run: bunx biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

pre-push:
  commands:
    check:
      run: bunx turbo typecheck test lint:ci colocated-tests secret-scan build
